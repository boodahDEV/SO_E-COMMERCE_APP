import { NbMenuItem } from '@nebular/theme';

export interface IdData<T> { // Mzero
    id: string;
    data: T;
}

export interface ModelNewChest { //MoTwo
    title?: string
    icon?: string;
    link?: string;
}
